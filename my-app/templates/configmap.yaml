apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  environment: {{ .Values.nodeSelector.environment | default "development" }}
  replicaCount: "{{ .Values.replicaCount }}"
